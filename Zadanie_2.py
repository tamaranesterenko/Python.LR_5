#!/usr/bin/env python3
# -*- coding: utf-8 -*-

if __name__ == '__main__':
    a = float(input("Enter a:"))
    b = float(input("Enter b:"))
    c = float(input("Enter c:"))

    if abs(a) > 4:
        print("Module a is more than 4")
    if abs(b) > 4:
        print("Module b is more than 4")
    if abs(c) > 4:
        print("Module c is more than 4")
